# Plan: standards conformance

Dependency-ordered tasks. Surface progress as **N/M** as you go; a "done" tone
before all tasks are consumed is not allowed.

- [ ] 1.
- [ ] 2.
- [ ] 3.

## Verification
<!-- The exact command(s) that prove each slice works (test / build / real run).
     Each task is "done" only when its verification has actually been run. -->
